<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.ensg.jpo.explor_descartes">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_lancement"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/icone_lancement_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SatisfactionActivity" />
        <activity
            android:name=".template"
            android:label="@string/title_activity_template" />
        <activity
            android:name=".PlanningActivity"
            android:label="@string/title_activity_planning" />
        <activity android:name=".EcoleActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".NavigationActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".AccueilActivity"
            android:label="@string/title_activity_accueil"
            android:theme="@style/AppTheme" />
        <activity android:name=".GestionCompte" />
        <activity android:name=".MoodifierPerso" />
        <activity android:name=".ModifierMdp" />
        <activity android:name=".ModifierPerso"></activity>
    </application>

</manifest>