<resources>

    <!-- Base application theme. -->
    <style name="Charte_Graphique" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorindetermine</item>
        <item name="colorAccent">@color/banderole</item>
        <item name="android:colorBackground">@color/colorBackgroung</item>
        <item name="android:colorForeground">@color/color</item>
        <item name="android:navigationBarColor">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/banderole</item>
        <item name="android:textColorPrimaryInverse">@android:color/black</item>
        <item name="android:textColorSecondary">@color/grisFonce</item>
        <item name="android:textColorSecondaryInverse">@android:color/darker_gray</item>
        <item name="android:windowBackground">@color/colorBackgroung</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
    </style>

</resources>
